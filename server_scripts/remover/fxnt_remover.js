ServerEvents.recipes(e => {
    // e.remove({ output: "fxntstorage:backpack_flight_upgrade" })
    e.remove({ output: "fxntstorage:backpack_magnet_upgrade" })
    // e.remove({ output: "fxntstorage:backpack_oremining_upgrade" })
})